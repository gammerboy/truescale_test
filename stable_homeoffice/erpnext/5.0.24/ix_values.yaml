persistence:
  worker:
    enabled: true
    # existingClaim: ""
    size: 8Gi
    storageClass: "gp2"
  logs:
    # Container based log search and analytics stack recommended
    enabled: false
    # existingClaim: ""
    size: 8Gi
    # storageClass: "openzfs"